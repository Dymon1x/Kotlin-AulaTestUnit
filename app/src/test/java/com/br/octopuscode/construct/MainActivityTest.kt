package com.br.octopuscode.construct

import androidx.arch.core.executor.testing.InstantTaskExecutorRule
import androidx.lifecycle.Observer
import com.nhaarman.mockitokotlin2.verify
import org.junit.Rule
import org.junit.Test
import org.junit.runner.RunWith
import org.mockito.Mock
import org.mockito.Mockito
import org.mockito.junit.MockitoJUnitRunner
import java.time.Instant
import java.util.*
import kotlin.collections.ArrayList
/*


@RunWith(MockitoJUnitRunner::class)
class MainActivityTest {

    private lateinit var viewModel: MainViewModel

    @Mock
    private lateinit var resListProds: Observer<ArrayList<Prod>>

    @get:Rule
    val rule = InstantTaskExecutorRule()

    //Quando chamar o viewModel fazemos um test
    @Test
    fun testQtdListProd() {
        viewModel = MainViewModel()
        viewModel.resListProds.observeForever(resListProds)
        var listProds = viewModel.defaultListProd()

        viewModel.setValueLiveDataProd(listProds)

        Mockito.verify(resListProds).onChanged(listProds)
//        Mockito.verify(resListProds).onChanged(defaultListProd())
        Mockito.verify((listProds.size == 9))
    }

        fun defaultListProd() = arrayListOf(
            Prod("Cimento", "Acabemento", 1, 32.0),
            Prod("Cimento", "Acabemento", 1, 32.0),
            Prod("Cimento", "Acabemento", 1, 32.0),
            Prod("Cimento", "Acabemento", 1, 32.0),
            Prod("Cimento", "Acabemento", 1, 32.0),
            Prod("Cimento", "Acabemento", 1, 32.0),
            Prod("Cimento", "Acabemento", 1, 32.0),
            Prod("Cimento", "Acabemento", 1, 32.0),
            Prod("Cimento", "Acabemento", 1, 32.0),
            )



    */
/*@Test
    fun `test`(){

    }*//*


}*/
